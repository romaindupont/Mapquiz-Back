const bcrypt = require('bcrypt')

/* const userVerificationPassword = (hashPassword, userPassword) => {
  //console.log(userPassword,hashPassword) */
  /* bcrypt.compare(userPassword, hashPassword), (err,res) => {
    if(res) {
      console.log(res)
      return res;
    } else {
      console.log('Your password not mached.');
    }
  } */
/* } */
/* module.exports = {
  userVerificationPassword,
} */